# Blockchain — Solidity + Sepolia

## Setup
```bash
npm install
npx hardhat compile
npx hardhat test

# Sepolia (testnet)
cp .env.example .env
npx hardhat compile
npx hardhat test
npm run deploy:sepolia
```
